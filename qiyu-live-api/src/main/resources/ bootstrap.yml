spring:
    application:
    name: qiyu-live-api
    cloud:
    nacos:
    username: ${nacos.nacos}
    password: ${nacos.password}
    discovery:
    server-addr: ${nacos.url}:8848
    namespace: ${nacos.namespace}
